# ─────────────────────────────────────────────────────────────────────────────
# ARGOS SLOPE 4.0 — ML Training & Inference Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Instalar en laptop/GPU para entrenamiento:
#   pip install -r ml/requirements.txt

ultralytics>=8.3.0
opencv-python>=4.10.0
numpy>=1.26.0
pyyaml>=6.0
torch>=2.4.0
onnx>=1.17.0
onnxruntime>=1.19.0
onnx2tf>=1.20.0      # opcional: para exportar a TFLite desde ONNX
tensorflow>=2.17.0    # opcional: para TFLite
