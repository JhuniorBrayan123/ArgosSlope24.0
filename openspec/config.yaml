# ═══════════════════════════════════════════════════════════════════════════════
# ARGOS SLOPE 4.0 — SDD Configuration
# ═══════════════════════════════════════════════════════════════════════════════
# Auto-generated by sdd-init on 2026-06-11
# Persistence mode: hybrid (Engram + openspec/)

context: >
  Mining slope deformation monitoring system. Multi-service architecture:
  Python edge pipeline (OpenCV camera capture + crack detection + MQTT publish),
  FastAPI backend REST API (dashboard data from crack_history.json),
  .NET 8 backend (Entity Framework + PostgreSQL + MQTT subscriber),
  Next.js 14 frontend (React 18, Three.js 3D viz, Recharts graphs, Tailwind CSS).
  ML training pipeline with YOLOv8 for crack classification. Deployed via Docker
  Compose with Postgres 16 and Mosquitto MQTT broker.

strict_tdd: true

testing:
  runner: pytest + unittest
  runner_command_python: "python -m pytest edge/edge/tests/ -v"
  runner_command_dotnet: "dotnet test backend/src/ArgosSlope.Api/"
  coverage_available: false
  coverage_command: null
  linter: "next lint (ESLint via Next.js)"
  type_checker: "TypeScript (tsc via next build)"
  formatter: null
  test_layers:
    unit:
      available: true
      tools:
        - "pytest (Python) — Edge pipeline unit tests (test_alert_engine.py, test_tracking.py, etc.)"
        - "unittest (Python) — legacy/mixed pattern in edge/tests/"
        - "dotnet test (.NET) — for C# backend services"
    integration:
      available: true
      tools:
        - "pytest + FastAPI TestClient — archived/backend/tests/"
    e2e:
      available: false
      tools: []
  warnings:
    - "No JavaScript test runner configured (Jest/Vitest/Playwright missing)"
    - "No coverage tool configured for any layer"
    - "Python linter/formatter not configured (ruff, black, flake8 absent)"
    - "Strict TDD set to true by detected runner fallback (pytest exists)"

phases:
  explore:
    prompt: "openspec/specs/{change}/explore.md"
  propose:
    prompt: "openspec/specs/{change}/propose.md"
  spec:
    prompt: "openspec/specs/{change}/spec.md"
  design:
    prompt: "openspec/specs/{change}/design.md"
  tasks:
    prompt: "openspec/specs/{change}/tasks.md"
  apply:
    prompt: null
    convention: "Implements task by task from tasks.md"
    test_first: true
  verify:
    prompt: null
    require_coverage: false
  archive:
    prompt: null
    archive_path: "openspec/changes/archive/{change}/"
